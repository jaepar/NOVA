services:
  redis:
    image: redis:7-alpine
    container_name: nova-redis-local
    restart: unless-stopped
    ports:
      - "6379:6379"
    command: ["redis-server", "--appendonly", "yes"]
    volumes:
      - redis-local-data:/data

volumes:
  redis-local-data:
